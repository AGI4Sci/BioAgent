# SciForge Workspace

This directory is the default local workspace for runtime state and generated task artifacts.

SciForge writes run outputs under `workspace/.sciforge/`, including task code copies, inputs, results, logs, sessions, artifacts, and versions. Those files are ignored by Git so the repository stays focused on source code, skills, tests, and documentation.
