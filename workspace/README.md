# BioAgent Workspace

This directory is the default local workspace for runtime state and generated task artifacts.

BioAgent writes run outputs under `workspace/.bioagent/`, including task code copies, inputs, results, logs, sessions, artifacts, and versions. Those files are ignored by Git so the repository stays focused on source code, skills, tests, and documentation.
